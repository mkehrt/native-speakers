# This is a basic workflow to help you get started with Actions

name: Deploy

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build-and-deploy:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - name: Build
        run: make

      - name: Set up SSH keys 
        env: 
          KNOWN_HOST: ${{ secrets.NFS_HOST_KEY_RSA }}
          SSH_AUTH_SOCK: "/tmp/ssh_agent.sock"
          SSH_KEY: ${{ secrets.NFS_SSH_KEY_RSA }}
        run: |
          mkdir -p ~/.ssh
          ssh-agent -a "$SSH_AUTH_SOCK"
          ssh-add -vvv - <<< "$SSH_KEY"
          echo "$KNOWN_HOST" >> ~/.ssh/known_hosts

      - name: Clean up old deploy
        env: 
          SSH_AUTH_SOCK: "/tmp/ssh_agent.sock"
        run: ssh -vvv mkehrt_nativespeakers@ssh.nyc1.nearlyfreespeech.net "find /home/public -mindepth 1 -maxdepth 1 |  xargs rm -rf"

      - name: Deploy
        env: 
          SSH_AUTH_SOCK: "/tmp/ssh_agent.sock"
        run: rsync -rv out/ mkehrt_nativespeakers@ssh.nyc1.nearlyfreespeech.net:/home/public

